namespace TSharpVision.Constants;

/// <summary>Capacity limits shared by file and directory dialogs.</summary>
public static class FileDialogConstants
{
    /// <summary>Path-input buffer capacity in character slots, including the slot reserved by input controls for termination.</summary>
    public const int MaxPathLen = 4096;
}
